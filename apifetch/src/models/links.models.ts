//An enum for each page that the site internally routes to
//Search is associated with the Games page as the games API endpoint requries a query/search
export enum Links{
    Home = "/",
    Deals = "/deals",
    Search = "/games", 
    Wishlist = "/wishlist"
}